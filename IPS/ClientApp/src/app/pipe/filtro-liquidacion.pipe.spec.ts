import { FiltroLiquidacionPipe } from './filtro-liquidacion.pipe';

describe('FiltroLiquidacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroLiquidacionPipe();
    expect(pipe).toBeTruthy();
  });
});
